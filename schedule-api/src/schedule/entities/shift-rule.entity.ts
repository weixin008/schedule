// 临时文件，用于解决导入错误
export enum ShiftPattern {
  FIXED = 'FIXED',
  ROTATING = 'ROTATING',
  FLEXIBLE = 'FLEXIBLE',
}

export enum WorkDayPattern {
  WEEKDAYS = 'WEEKDAYS',
  WEEKENDS = 'WEEKENDS',
  ALL_DAYS = 'ALL_DAYS',
  CUSTOM = 'CUSTOM',
}